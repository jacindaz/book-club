<h3>Submit a Review</h3>

<%= simple_form_for([@book,@review]) do |f| %>
  <%= f.input :rating, collection: [5,4,3,2,1], prompt: "Select a Rating" %>
  <%= f.input :body, label: "Your Review", as: :text %>
  <%= f.button :submit, "Submit Your Review" %>
<% end %>
